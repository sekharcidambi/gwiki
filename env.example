components/EnhancedWikiGenerator.tsx (178:21) @ EnhancedWikiGenerator

  176 | const currentPage = selectedPage 
  177 |   ? wikiData.pages.find(p => p.path === selectedPage)
> 178 |   : wikiData.pages[0]
      |                   ^
  179 |
  180 | const toggleSection = (sectionTitle: string) => {
  181 |   const newExpanded = new Set(expandedSections)
  